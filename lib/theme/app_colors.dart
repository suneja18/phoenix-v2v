// app_colors.dart
//
// Single source of truth for the app's color palette.
// Import this file wherever you need a color instead of hardcoding hex values,
// so the whole app stays consistent and easy to re-theme later.

import 'package:flutter/material.dart';

class AppColors {
  static const cream = Color(0xFFFCFEEE);
  static const lightBlue = Color(0xFFD3E1EE);
  static const navy = Color(0xFF154D7B);
}
